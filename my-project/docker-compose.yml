version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./src/app:/app
    environment:
      - NODE_ENV=development
    networks:
      - my-network

networks:
  my-network:
    driver: bridge